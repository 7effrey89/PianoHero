# Flask Configuration
PORT=5000
FLASK_ENV=development

# Demo Fallback Configuration
# Set to 'true' to enable fallback to demo notes when real pitch detection is not available
# Set to 'false' to disable demo fallbacks and return errors instead
ENABLE_DEMO_FALLBACK=true
